{
  "name": "html",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.8.3"
  },
  "scripts": {
    "copy-img": "cp -vR ./src/images ./dist && cp -vR ./src/icons ./dist",
    "copy-json": "cp -vR ./src/data.json ./dist",
    "start": "parcel src/index.html"
  }
}
